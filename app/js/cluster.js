const Pixel = require("./pixel");


class Cluster {
    pixels

    AddPixel(p) {
        this.pixels.push(p);
    }

    EvaluateMidpointPixelValue() {
        return 
    }
}

module.exports.Cluster = Cluster;